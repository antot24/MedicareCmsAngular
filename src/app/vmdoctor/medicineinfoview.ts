export class Medicineinfoview {

    MedicineId: number;
    MedicineName: string;
    GenericName: string;
    ManufacturingDate: string;  // Use `string` or `Date` based on your requirement
    ExpiryDate: string;         // Use `string` or `Date` based on your requirement
    Category: string;
    Quantity: number;
    PricePerUnit: number;
    CreatedDate: string;   
}
