import { Testprescriptionview } from './testprescriptionview';

describe('Testprescriptionview', () => {
  it('should create an instance', () => {
    expect(new Testprescriptionview()).toBeTruthy();
  });
});
