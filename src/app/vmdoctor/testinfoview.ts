export class Testinfoview {
    TestId: number;
  TestName: string;
  TestType: string;
  Category: string;
  Amount: number;
  ReferenceMinRange: string;   // Can also be a number depending on use case
  ReferenceMaxRange: string;   // Can also be a number depending on use case
  SampleRequired: string;
  CreatedDate: string;  
}
