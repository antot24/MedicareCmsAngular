import { Testprescriptionlist } from './testprescriptionlist';

describe('Testprescriptionlist', () => {
  it('should create an instance', () => {
    expect(new Testprescriptionlist()).toBeTruthy();
  });
});
