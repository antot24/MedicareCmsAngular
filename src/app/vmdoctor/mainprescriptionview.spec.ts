import { Mainprescriptionview } from './mainprescriptionview';

describe('Mainprescriptionview', () => {
  it('should create an instance', () => {
    expect(new Mainprescriptionview()).toBeTruthy();
  });
});
