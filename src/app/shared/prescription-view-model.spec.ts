import { PrescriptionViewModel } from './prescription-view-model';

describe('PrescriptionViewModel', () => {
  it('should create an instance', () => {
    expect(new PrescriptionViewModel()).toBeTruthy();
  });
});
