import { DoctorTokenTrackerR } from './doctor-token-tracker-r';

describe('DoctorTokenTrackerR', () => {
  it('should create an instance', () => {
    expect(new DoctorTokenTrackerR()).toBeTruthy();
  });
});
