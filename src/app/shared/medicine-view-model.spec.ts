import { MedicineViewModel } from './medicine-view-model';

describe('MedicineViewModel', () => {
  it('should create an instance', () => {
    expect(new MedicineViewModel()).toBeTruthy();
  });
});
